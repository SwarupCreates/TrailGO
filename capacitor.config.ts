import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.gpxnavigator',
  appName: 'offline-gpx-navigation',
  webDir: 'dist'
};

export default config;
